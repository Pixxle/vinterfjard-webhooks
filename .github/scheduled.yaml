name: Hourly cron job
on:
  schedule:
    - cron: "0 */12 * * *"
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Hourly cron job
        run: |
          curl --request POST \
          --url 'https://vinterfjard-webhooks.vercel.app/riksarkivet' \
          --header 'Authorization: Bearer ${{ secrets.ACTION_KEY }}'
